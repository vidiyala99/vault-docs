# Secrets & local config
.env
.env.*
!.env.example

# Private working notes (local-only Claude Code context)
CLAUDE.md
.remember/
.claude/

# Assignment material (not ours to redistribute)
*.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Local storage / artifacts (uploaded docs live here in dev).
# Anchored to repo root: an unanchored "storage/" also ignored app/storage/,
# which silently kept the whole storage module out of the repo until CI's
# first clean-clone run caught the missing import.
/storage/
*.db

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
